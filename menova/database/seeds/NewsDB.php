<?php

use Illuminate\Database\Seeder;
use App\News;
class NewsDB extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        // for($i=0; $i <= 10; $i++)
        // {
        // 	$add = new News;
        // 	$add->title = 'new title'.rand(0,9);
        // 	$add->add_by = 1;
        // 	$add->content = 'welcome to new user pages' . rand(0,9);
        // 	$add->description = "welcome it is adescription file".rand(0, 9);
        // 	$add->state = "active";
        // 	$add->save();
        // }
    }
}
